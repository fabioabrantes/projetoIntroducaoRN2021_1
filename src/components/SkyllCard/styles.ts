import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: {
    backgroundColor: '#1f1e66',
    borderRadius: 20,
    padding: 15,
    alignItems: 'center',
    marginTop: 20,
  },
  buttonTitle: {
    color: '#fff',
    fontWeight: 'bold',
    fontSize: 18,
  },
});
export {styles};
